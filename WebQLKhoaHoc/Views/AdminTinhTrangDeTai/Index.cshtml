@model IEnumerable<WebQLKhoaHoc.TinhTrangDeTai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTinhTrang)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTinhTrang)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaTinhTrang }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaTinhTrang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaTinhTrang })
        </td>
    </tr>
}

</table>
