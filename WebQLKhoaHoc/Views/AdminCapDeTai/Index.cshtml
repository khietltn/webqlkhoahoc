@model IEnumerable<WebQLKhoaHoc.CapDeTai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCapDeTai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCapDeTai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCapDeTai }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCapDeTai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCapDeTai })
        </td>
    </tr>
}

</table>
