@model IEnumerable<WebQLKhoaHoc.NhomLinhVuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNhomLinhVuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNhomLinhVuc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNhomLinhVuc }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNhomLinhVuc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNhomLinhVuc })
        </td>
    </tr>
}

</table>
