@model IEnumerable<WebQLKhoaHoc.LoaiHinhDeTai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiDT }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiDT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiDT })
        </td>
    </tr>
}

</table>
