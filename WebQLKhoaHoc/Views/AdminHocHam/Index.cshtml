@model IEnumerable<WebQLKhoaHoc.HocHam>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenVietTat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenHocHam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenVietTat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenHocHam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHocHam }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHocHam }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHocHam })
        </td>
    </tr>
}

</table>
