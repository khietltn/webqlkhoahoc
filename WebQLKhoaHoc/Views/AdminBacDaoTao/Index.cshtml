@model IEnumerable<WebQLKhoaHoc.BacDaoTao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenBacDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenBacDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaBacDT }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaBacDT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaBacDT })
        </td>
    </tr>
}

</table>
