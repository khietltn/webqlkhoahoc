@model IEnumerable<WebQLKhoaHoc.PhanLoaiSP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenPhanLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenPhanLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaPhanLoai }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaPhanLoai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaPhanLoai })
        </td>
    </tr>
}

</table>
