@model IEnumerable<WebQLKhoaHoc.NgachVienChuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNgach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNgach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNgach }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNgach }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNgach })
        </td>
    </tr>
}

</table>
