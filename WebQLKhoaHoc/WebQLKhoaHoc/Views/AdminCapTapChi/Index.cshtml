@model IEnumerable<WebQLKhoaHoc.CapTapChi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCapTapChi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChiChu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCapTapChi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChiChu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCapTapChi }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCapTapChi }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCapTapChi })
        </td>
    </tr>
}

</table>
