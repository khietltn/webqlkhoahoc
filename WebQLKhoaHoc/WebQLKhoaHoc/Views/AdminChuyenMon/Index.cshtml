@model IEnumerable<WebQLKhoaHoc.ChuyenMon>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenChuyenMon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChuyenMon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaChuyenMon }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaChuyenMon }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaChuyenMon })
        </td>
    </tr>
}

</table>
