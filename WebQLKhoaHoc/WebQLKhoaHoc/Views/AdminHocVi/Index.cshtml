@model IEnumerable<WebQLKhoaHoc.HocVi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenVietTat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenHocVi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenVietTat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenHocVi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHocVi }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHocVi }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHocVi })
        </td>
    </tr>
}

</table>
