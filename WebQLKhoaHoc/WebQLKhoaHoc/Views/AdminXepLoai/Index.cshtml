@model IEnumerable<WebQLKhoaHoc.XepLoai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenXepLoai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenXepLoai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaXepLoai }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaXepLoai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaXepLoai })
        </td>
    </tr>
}

</table>
